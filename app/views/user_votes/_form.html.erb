<section class="content invoice">
  <div class="row">
    <div class="col-md-10">
      <%= form_for(@user_vote, :html => { :role => "form" }) do |f| %>
        <% if @user_vote.errors.any? %>
          <div class="alert alert-danger alert-dismissable" role="alert">
            <button type="button" class="close" data-dismiss="alert" aria-hidden="true">x</button>
            <h4><%= pluralize(@user_vote.errors.count, "error") %> prohibited this user_vote from being saved:</h4>

            <ul>
            <% @user_vote.errors.full_messages.each do |msg| %>
              <li><%= msg %></li>
            <% end %>
            </ul>
          </div>
        <% end %>

      <div class="form-group">
        <%= f.label :user_id %>
        <%= f.number_field :user_id, :class => "form-control" %>
      </div>
      <div class="form-group">
        <%= f.label :joke_id %>
        <%= f.number_field :joke_id, :class => "form-control" %>
      </div>
      <div class="form-group">
        <%= f.submit :class => "btn btn-flat bg-olive" %>
      </div>
    <% end %>
    </div>
  </div>
</section> 