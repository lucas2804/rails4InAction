<section class="content invoice">
  <div class="row">
    <div class="col-md-10">
      <%= form_for(@admin_book, :html => { :role => "form" }) do |f| %>
        <% if @admin_book.errors.any? %>
          <div class="alert alert-danger alert-dismissable" role="alert">
            <button type="button" class="close" data-dismiss="alert" aria-hidden="true">x</button>
            <h4><%= pluralize(@admin_book.errors.count, "error") %> prohibited this admin_book from being saved:</h4>

            <ul>
            <% @admin_book.errors.full_messages.each do |msg| %>
              <li><%= msg %></li>
            <% end %>
            </ul>
          </div>
        <% end %>

      <div class="form-group">
        <%= f.submit :class => "btn btn-flat bg-olive" %>
      </div>
    <% end %>
    </div>
  </div>
</section> 