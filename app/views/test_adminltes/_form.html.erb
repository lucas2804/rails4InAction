<section class="content invoice">
  <div class="row">
    <div class="col-md-10">
      <%= form_for(@test_adminlte, :html => { :role => "form" }) do |f| %>
        <% if @test_adminlte.errors.any? %>
          <div class="alert alert-danger alert-dismissable" role="alert">
            <button type="button" class="close" data-dismiss="alert" aria-hidden="true">x</button>
            <h4><%= pluralize(@test_adminlte.errors.count, "error") %> prohibited this test_adminlte from being saved:</h4>

            <ul>
            <% @test_adminlte.errors.full_messages.each do |msg| %>
              <li><%= msg %></li>
            <% end %>
            </ul>
          </div>
        <% end %>

      <div class="form-group">
        <%= f.submit :class => "btn btn-flat bg-olive" %>
      </div>
    <% end %>
    </div>
  </div>
</section> 